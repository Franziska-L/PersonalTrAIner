//set height and width of the video frame
//Set it to 800 when camera is on
const videoWidth = 700;
const videoHeight = 400;

const maxNumOfRep = 15;
const maxNumOfSets = 3;
const restTime = 59;

module.exports = {
    videoHeight,
    videoWidth,
    maxNumOfRep,
    maxNumOfSets,
    restTime,
}